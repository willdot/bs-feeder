export const pick = (obj, keys) => {
    const cloned = {};
    for (let idx = 0, len = keys.length; idx < len; idx++) {
        const key = keys[idx];
        // @ts-expect-error
        cloned[key] = obj[key];
    }
    return cloned;
};
//# sourceMappingURL=misc.js.map